<UserControl x:Class="FootballApp.Views.LoaderOfStatForMatchesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FootballApp.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Border Width="350" Height="100" Padding="10 5">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" VerticalAlignment="Center">
                    <Run Text="H2H Count:"/>
                    <Run Text="{Binding H2hCount, Mode=OneWay}"/>
                </TextBlock>
                <TextBlock Grid.Row="1" VerticalAlignment="Center">
                    <Run Text="StatAndSumm Count:"/>
                    <Run Text="{Binding StatAndSummCount, Mode=OneWay}"/>
                </TextBlock>
            </Grid>
        </Border>
    </Grid>
</UserControl>
